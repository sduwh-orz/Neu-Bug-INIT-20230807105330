<?xml version="1.0" encoding="UTF-8"?>
<project name="AntMavenPackage" basedir="." default="build">

    <!-- Default target that calls the Maven package goal -->
    <target name="build">
        <exec executable="mvn">
            <arg value="package" />
        </exec>
    </target>

</project>